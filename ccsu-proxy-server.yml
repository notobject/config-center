tag: 20181204-14
zuul:
  routes:
    api-user:
      path: /user/**
      service-id: demo-user-service
      sensitive-headers:
    api-settings:
      path: /settings/**
      service-id: demo-settings-service

headers: appPlatform,appVersion,sessionId
