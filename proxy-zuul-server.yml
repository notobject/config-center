version: 5
zuul:
  routes:
    api-user:
      path: /api/user/**
      service-id: demo-user-service
      sensitive-headers:
    api-settings:
      path: /api/settings/**
      service-id: demo-settings-service
management:
  endpoints:
    web:
      exposure:
        include: "*"
